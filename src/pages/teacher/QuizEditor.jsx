import { useEffect, useState } from 'react';
import { Link, useNavigate, useParams, useSearchParams } from 'react-router-dom';
import { AddIcon, AiAssistIcon, DeleteIcon, MaterialIcon } from '../../components/icons';
import { Button, Card, Input, PageHeader, ProcessLoader, Textarea, buttonClassName } from '../../components/ui';
import AiAssistModal from '../../components/AiAssistModal';
import { getClass } from '../../services/class.service';
import { askQuizzyAi } from '../../services/ai.service';
import { listLearningSessions } from '../../services/learning-session.service';
import { createGeneralQuiz, createQuiz, getGeneralQuiz, getQuiz, quizErrorMessage, updateGeneralQuiz, updateQuiz } from '../../services/quiz.service';
import { uploadErrorMessage, uploadMaterialAsset } from '../../services/upload.service';

const newQuestion = (index) => ({ id: `q-${Date.now()}-${index}`, type: 'multiple_choice', prompt: '', choices: ['', ''], correctAnswer: '', items: [], correctOrder: [], imageUrl: '', hotspots: [], tolerancePercent: 2, explanation: '', points: 1 });
const typeDefaults = (type) => ({ type, choices: type === 'multiple_choice' ? ['', ''] : [], correctAnswer: type === 'true_false' ? true : '', items: type === 'arrange' ? [{ id: 'item-1', text: '' }, { id: 'item-2', text: '' }, { id: 'item-3', text: '' }] : [], correctOrder: type === 'arrange' ? ['item-1', 'item-2', 'item-3'] : [], imageUrl: '', hotspots: [], tolerancePercent: 2 });

function QuestionImage({ question, change, classId, general }) {
  const [uploading, setUploading] = useState(false); const [error, setError] = useState('');
  const upload = async (file) => { if (!file) return; setUploading(true); setError(''); try { const asset = await uploadMaterialAsset(classId, file, { generalQuiz: general }); change({ imageUrl: asset.url }); } catch (caught) { setError(uploadErrorMessage(caught)); } finally { setUploading(false); } };
  return <div className="qz-question-image"><Input label="Gambar soal (opsional)" type="url" value={question.imageUrl || ''} placeholder="https://.../gambar.png" onChange={(event) => change({ imageUrl: event.target.value })} />
    <label className="qz-upload-control"><span><MaterialIcon size={16} /> Unggah gambar</span><input type="file" accept="image/jpeg,image/png,image/webp,image/gif" disabled={uploading} onChange={(event) => upload(event.target.files?.[0])} /><small>{uploading ? 'Mengunggah gambar...' : 'JPG, PNG, WebP, atau GIF · maks. 8 MB · rasio asli tetap terjaga'}</small></label>
    {error ? <p className="qz-field__error" role="alert">{error}</p> : null}
    {question.imageUrl ? <figure><img src={question.imageUrl} alt="Pratinjau gambar soal" onError={(event) => { event.currentTarget.style.display = 'none'; }} /><figcaption>Gambar ditampilkan tanpa dipotong. Ubah tautan bila ingin menggantinya.</figcaption></figure> : null}
  </div>;
}

function ArrangeEditor({ question, change }) {
  const items = question.items || [];
  const update = (next) => change({ items: next, correctOrder: next.map((item) => item.id) });
  const move = (from, to) => { const next = [...items]; const [item] = next.splice(from, 1); next.splice(to, 0, item); update(next); };
  return <div className="qz-arrange-editor"><span>Susun setiap langkah ke urutan jawaban benar. Siswa akan menerimanya dalam urutan acak.</span>{items.map((item, itemIndex) => <div className="qz-arrange-editor__item" draggable key={item.id} onDragStart={(event) => event.dataTransfer.setData('text/plain', String(itemIndex))} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); move(Number(event.dataTransfer.getData('text/plain')), itemIndex); }}><b>{itemIndex + 1}</b><input value={item.text} placeholder={`Langkah ${itemIndex + 1}`} onChange={(event) => update(items.map((current, index) => index === itemIndex ? { ...current, text: event.target.value } : current))} /><button type="button" disabled={itemIndex === 0} onClick={() => move(itemIndex, itemIndex - 1)}>↑</button><button type="button" disabled={itemIndex === items.length - 1} onClick={() => move(itemIndex, itemIndex + 1)}>↓</button></div>)}<Button size="sm" variant="secondary" disabled={items.length >= 10} onClick={() => update([...items, { id: `item-${Date.now()}`, text: '' }])}>Tambah langkah</Button></div>;
}

function HotspotEditor({ question, change }) {
  const [drawing, setDrawing] = useState(null); const hotspots = question.hotspots || [];
  const point = (event) => { const box = event.currentTarget.getBoundingClientRect(); return { x: Math.max(0, Math.min(100, ((event.clientX - box.left) / box.width) * 100)), y: Math.max(0, Math.min(100, ((event.clientY - box.top) / box.height) * 100)) }; };
  const finish = (event) => { if (!drawing) return; const end = point(event); const x = Math.min(drawing.x, end.x); const y = Math.min(drawing.y, end.y); const width = Math.abs(drawing.x - end.x); const height = Math.abs(drawing.y - end.y); setDrawing(null); if (width < 2 || height < 2 || hotspots.length >= 8) return; change({ hotspots: [...hotspots, { id: `area-${Date.now()}`, label: `Area ${hotspots.length + 1}`, x, y, width, height, correct: !hotspots.some((area) => area.correct) }] }); };
  return <div className="qz-hotspot-editor"><Input label="Toleransi tepi area (%)" type="number" min="0" max="10" value={question.tolerancePercent ?? 2} onChange={(event) => change({ tolerancePercent: Number(event.target.value) })} /><p>Tarik pada gambar untuk membentuk blok jawaban. Garis ungu adalah area dinilai benar, termasuk toleransinya.</p>{question.imageUrl ? <div className="qz-hotspot-canvas" onPointerDown={(event) => setDrawing(point(event))} onPointerUp={finish} onPointerCancel={() => setDrawing(null)}><img src={question.imageUrl} alt="Pratinjau area klik" />{hotspots.map((area) => <button type="button" title="Pilih sebagai jawaban benar" className={`qz-hotspot-block${area.correct ? ' is-correct' : ''}`} key={area.id} style={{ left: `${area.x}%`, top: `${area.y}%`, width: `${area.width}%`, height: `${area.height}%` }} onClick={(event) => { event.stopPropagation(); change({ hotspots: hotspots.map((current) => ({ ...current, correct: current.id === area.id })) }); }}><span>{area.label}</span></button>)}{drawing ? <i className="qz-hotspot-draft" style={{ left: `${drawing.x}%`, top: `${drawing.y}%` }} /> : null}</div> : <div className="qz-hotspot-empty">Unggah atau masukkan URL gambar terlebih dahulu.</div>}<div className="qz-hotspot-list">{hotspots.map((area, index) => <label key={area.id}><input type="radio" name={`hotspot-${question.id}`} checked={area.correct} onChange={() => change({ hotspots: hotspots.map((current) => ({ ...current, correct: current.id === area.id })) })} /> <input value={area.label} onChange={(event) => change({ hotspots: hotspots.map((current, currentIndex) => currentIndex === index ? { ...current, label: event.target.value } : current) })} /> <button type="button" onClick={() => change({ hotspots: hotspots.filter((current) => current.id !== area.id) })}>Hapus</button></label>)}</div></div>;
}

export default function QuizEditor({ scope = 'class' }) {
  const { classId, quizId } = useParams(); const navigate = useNavigate(); const [searchParams] = useSearchParams(); const general = scope === 'general'; const editing = Boolean(quizId);
  const backPath = general ? '/teacher/general-quizzes' : `/teacher/classes/${classId}/quizzes`;
  const [className, setClassName] = useState('');
  const [sessions, setSessions] = useState([]);
  const [form, setForm] = useState({ title: '', description: '', status: 'draft', ...(general ? {} : { sessionId: searchParams.get('sessionId') || '' }), settings: { shuffleQuestions: false, showCorrectAnswers: true, passingScore: 70 }, questions: [newQuestion(0)] }); const [busy, setBusy] = useState(editing); const [error, setError] = useState('');
  useEffect(() => { if (general || !classId) return undefined; const controller = new AbortController(); Promise.all([getClass(classId, { signal: controller.signal }), listLearningSessions(classId, { signal: controller.signal })]).then(([item, sessionItems]) => { setClassName(item.name || ''); setSessions(sessionItems); }).catch(() => setClassName('')); return () => controller.abort(); }, [classId, general]);
  useEffect(() => { if (!editing) return undefined; const controller = new AbortController(); const load = general ? getGeneralQuiz(quizId, { signal: controller.signal }) : getQuiz(classId, quizId, { signal: controller.signal }); load.then((quiz) => { setForm(general ? quiz : { ...quiz, sessionId: quiz.sessionId || '' }); setBusy(false); }).catch((caught) => { if (caught.name !== 'AbortError') { setError(quizErrorMessage(caught)); setBusy(false); } }); return () => controller.abort(); }, [classId, quizId, editing, general]);
  const changeQuestion = (index, patch) => setForm((current) => ({ ...current, questions: current.questions.map((question, currentIndex) => currentIndex === index ? { ...question, ...patch } : question) }));
  const save = async (status) => { setBusy(true); setError(''); try { const payload = { ...form, status }; const saved = editing ? general ? await updateGeneralQuiz(quizId, payload) : await updateQuiz(classId, quizId, payload) : general ? await createGeneralQuiz(payload) : await createQuiz(classId, payload); navigate(general ? `/teacher/general-quizzes/${saved.id}/edit` : `/teacher/classes/${classId}/quizzes/${saved.id}/edit`, { replace: true }); setForm(saved); } catch (caught) { setError(quizErrorMessage(caught)); } finally { setBusy(false); } };
  const generateByAi = async ({ context, instruction }) => { setBusy(true); setError(''); try { const response = await askQuizzyAi({ task: 'quiz_draft', context, instruction }); const parsed = JSON.parse(response.content); const generated = Array.isArray(parsed.questions) ? parsed.questions.slice(0, 15).map((item, index) => { const type = ['multiple_choice', 'true_false', 'short_answer', 'arrange'].includes(item.type) ? item.type : 'multiple_choice'; const items = type === 'arrange' && Array.isArray(item.items) ? item.items.slice(0, 10).map((entry, itemIndex) => ({ id: `item-${itemIndex + 1}`, text: String(entry?.text || entry || '').slice(0, 300) })).filter((entry) => entry.text) : []; const choices = type === 'multiple_choice' && Array.isArray(item.choices) ? [...new Set(item.choices.map(String).map((choice) => choice.slice(0, 300)).filter(Boolean))].slice(0, 5) : ['', '']; const correctAnswer = type === 'true_false' ? Boolean(item.correctAnswer) : type === 'multiple_choice' && choices.includes(String(item.correctAnswer)) ? String(item.correctAnswer) : type === 'short_answer' ? String(item.correctAnswer || '') : ''; return { ...newQuestion(index), id: `ai-${Date.now()}-${index}`, type, prompt: String(item.prompt || '').slice(0, 1000), choices, correctAnswer, items, correctOrder: type === 'arrange' ? items.map((entry) => entry.id) : [], explanation: String(item.explanation || '').slice(0, 1000), points: Number.isInteger(item.points) ? item.points : 1 }; }) : []; if (!generated.length) throw new Error('AI tidak mengembalikan soal yang dapat digunakan.'); setForm((current) => ({ ...current, questions: [...current.questions.filter((question) => question.prompt || question.choices.some(Boolean)), ...generated].slice(0, 30) })); setAiModalOpen(false); } catch (caught) { setError(caught.message || 'Draf AI belum dapat dibuat.'); } finally { setBusy(false); } };
  if (busy && editing && !form.title) return <div className="qz-dashboard"><div className="qz-status-strip">Memuat editor kuis...</div></div>;
  return <div className="qz-dashboard qz-enter"><PageHeader eyebrow={general ? 'Studio kuis umum' : `Studio kuis${className ? ` · ${className}` : ''}`} title={editing ? 'Edit kuis' : 'Kuis baru'} description="Pilih format yang paling cocok, tandai jawaban benar secara visual, lalu simpan draf atau terbitkan." actions={<div style={{ display: 'flex', gap: '8px' }}><Button variant="secondary" onClick={() => setAiModalOpen(true)}><AiAssistIcon size={16} /> AI Assist</Button><Link className={buttonClassName({ variant: 'ghost' })} to={backPath}>Kembali</Link></div>} />{error ? <div className="qz-inline-state qz-inline-state--error">{error}</div> : null}<div className="qz-quiz-editor"><Card className="qz-quiz-editor__main"><Input label="Judul kuis" value={form.title} maxLength={120} onChange={(event) => setForm({ ...form, title: event.target.value })} /><Textarea label="Deskripsi" value={form.description} maxLength={500} onChange={(event) => setForm({ ...form, description: event.target.value })} />{!general ? <label className="qz-field"><span>Pertemuan (opsional)</span><select value={form.sessionId || ''} onChange={(event) => setForm({ ...form, sessionId: event.target.value })}><option value="">Tanpa pertemuan</option>{sessions.map((session) => <option key={session.id} value={session.id} disabled={session.status === 'archived' && session.id !== form.sessionId}>{session.meetingDate} · {session.title}{session.status === 'draft' ? ' (Draf)' : session.status === 'archived' ? ' (Arsip)' : ''}</option>)}</select></label> : null}<div className="qz-question-list">{form.questions.map((question, index) => <section className="qz-question" key={question.id}><div className="qz-question__head"><strong>Soal {index + 1}</strong><Button variant="ghost" size="sm" disabled={form.questions.length === 1} onClick={() => setForm({ ...form, questions: form.questions.filter((_, currentIndex) => currentIndex !== index) })}><DeleteIcon size={16} /> Hapus</Button></div><label className="qz-field"><span>Jenis soal</span><select value={question.type} onChange={(event) => changeQuestion(index, typeDefaults(event.target.value))}><option value="multiple_choice">Pilihan ganda</option><option value="true_false">Benar atau salah</option><option value="short_answer">Jawaban singkat</option><option value="arrange">Susun urutan</option><option value="image_hotspot">Klik area gambar</option></select></label><Textarea label="Pertanyaan" value={question.prompt} maxLength={1000} onChange={(event) => changeQuestion(index, { prompt: event.target.value })} /><QuestionImage question={question} change={(patch) => changeQuestion(index, patch)} classId={classId} general={general} />{question.type === 'multiple_choice' ? <div className="qz-choice-list">{question.choices.map((choice, choiceIndex) => <label key={choiceIndex}><input type="radio" name={`correct-${question.id}`} checked={question.correctAnswer === choice && Boolean(choice)} onChange={() => changeQuestion(index, { correctAnswer: choice })} /><input value={choice} placeholder={`Pilihan ${choiceIndex + 1}`} onChange={(event) => { const choices = [...question.choices]; choices[choiceIndex] = event.target.value; changeQuestion(index, { choices, correctAnswer: question.correctAnswer === choice ? event.target.value : question.correctAnswer }); }} /></label>)}</div> : question.type === 'true_false' ? <label className="qz-field"><span>Jawaban benar</span><select value={String(question.correctAnswer)} onChange={(event) => changeQuestion(index, { correctAnswer: event.target.value === 'true' })}><option value="true">Benar</option><option value="false">Salah</option></select></label> : question.type === 'short_answer' ? <Input label="Jawaban benar" value={question.correctAnswer} onChange={(event) => changeQuestion(index, { correctAnswer: event.target.value })} /> : question.type === 'arrange' ? <ArrangeEditor question={question} change={(patch) => changeQuestion(index, patch)} /> : <HotspotEditor question={question} change={(patch) => changeQuestion(index, patch)} />}<div className="qz-question__row"><Input label="Poin" type="number" min="1" max="100" value={question.points} onChange={(event) => changeQuestion(index, { points: Number(event.target.value) })} /><Input label="Pembahasan (opsional)" value={question.explanation} onChange={(event) => changeQuestion(index, { explanation: event.target.value })} /></div></section>)}</div><Button variant="secondary" onClick={() => setForm({ ...form, questions: [...form.questions, newQuestion(form.questions.length)] })}><AddIcon size={17} /> Tambah soal</Button></Card><Card className="qz-quiz-settings"><h3>Pengaturan</h3><Input label="Nilai kelulusan" type="number" min="0" max="100" value={form.settings.passingScore} onChange={(event) => setForm({ ...form, settings: { ...form.settings, passingScore: Number(event.target.value) } })} /><label><input type="checkbox" checked={form.settings.shuffleQuestions} onChange={(event) => setForm({ ...form, settings: { ...form.settings, shuffleQuestions: event.target.checked } })} /> Acak urutan soal</label><label><input type="checkbox" checked={form.settings.showCorrectAnswers} onChange={(event) => setForm({ ...form, settings: { ...form.settings, showCorrectAnswers: event.target.checked } })} /> Tampilkan jawaban benar</label><Button disabled={busy} onClick={() => save('published')}>{busy ? <><ProcessLoader size={18} label="Menyimpan kuis" /> Menyimpan...</> : 'Terbitkan'}</Button><Button variant="secondary" disabled={busy} onClick={() => save('draft')}>Simpan draf</Button></Card></div>
  <AiAssistModal open={aiModalOpen} onClose={() => setAiModalOpen(false)} module="quiz" initialContext={{ title: form.title, context: form.description }} onApply={generateByAi} busy={busy} />
  </div>;
}div>;
}
